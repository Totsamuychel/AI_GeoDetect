2. Примеры фото для быстрого старта
Добавь 4–6 готовых примеров ("нажми и протестируй") — по одному на каждый город + один OOD-пример (например, Берлин или Рим). Пользователь сразу видит как работает модель без поиска своего фото.

3. Интерактивная карта с результатом
После предсказания показывать Leaflet/MapLibre карту с маркером на предсказанном городе и радиусом уверенности — визуально намного убедительнее, чем просто "Варшава 80.7%".

Функциональность модели
4. Heatmap / Grad-CAM визуализация
Показывай, на что смотрела модель при классификации — выделение патчей ViT, которые дали наибольший вес. Это и объяснимость AI, и просто впечатляющая фича для диплома.

5. OOD-объяснение с деталями
Когда OOD-гейт срабатывает — сейчас просто "не схоже". Добавь: какой порог был, какой score получило фото, и рядом предложение "возможно это [GeoCLIP-предположение]?" Используй GeoCLIP как fallback для подсказки.

6. Сравнение всех трёх моделей одновременно
Сейчас переключение между StreetCLIP / EfficientNet / GeoCLIP раздельное. Добавь режим "Запустить всё" — таблица с тремя результатами рядом, чтобы видеть разницу в confidence.

10. Бенчмарк-таблица с метриками
Добавь раздел с confusion matrix и Accuracy/F1/TPR/FPR для каждой из трёх моделей в виде интерактивной таблицы. Это критически важно для диплома и сразу отвечает на вопрос "какая модель лучше и почему".