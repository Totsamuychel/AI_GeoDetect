# =============================================================================
# requirements.txt — pip equivalent of environment.yml
# Install with:  pip install -r requirements.txt
#
# NOTE: PyTorch with CUDA 11.8 must be installed separately:
#   pip install torch==2.1.* torchvision==0.16.* torchaudio==2.1.* \
#       --index-url https://download.pytorch.org/whl/cu118
# =============================================================================

# ── PyTorch (install via URL above; listed here for reference) ─────────────
# torch==2.1.*
# torchvision==0.16.*
# torchaudio==2.1.*

# ── Core scientific stack ───────────────────────────────────────────────────
numpy>=1.24,<2.0
pandas>=2.0,<3.0
scipy>=1.11,<2.0
scikit-learn>=1.3,<2.0
matplotlib>=3.7,<4.0
seaborn>=0.12,<1.0

# ── HuggingFace ecosystem ───────────────────────────────────────────────────
transformers>=4.38,<5.0
datasets>=2.17,<3.0
accelerate>=0.27,<1.0
tokenizers>=0.15,<1.0
huggingface-hub>=0.20,<1.0

# ── CLIP ────────────────────────────────────────────────────────────────────
openai-clip>=1.0.1
open-clip-torch>=2.24,<3.0

# ── Geospatial ──────────────────────────────────────────────────────────────
geopandas>=0.14,<1.0
shapely>=2.0,<3.0
pyproj>=3.6,<4.0
geopy>=2.4,<3.0
folium>=0.15,<1.0
h3>=3.7,<4.0
branca>=0.7,<1.0

# ── Image processing ────────────────────────────────────────────────────────
Pillow>=10.0,<11.0
opencv-python>=4.8,<5.0
albumentations>=1.3,<2.0

# ── Experiment tracking ─────────────────────────────────────────────────────
wandb>=0.16,<1.0
mlflow>=2.10,<3.0
optuna>=3.5,<4.0

# ── Model backbones & utilities ─────────────────────────────────────────────
timm>=0.9,<1.0
einops>=0.7,<1.0

# ── Explainability & visualisation ──────────────────────────────────────────
grad-cam>=1.5,<2.0
plotly>=5.18,<6.0
kaleido>=0.2,<0.3

# ── Training utilities ───────────────────────────────────────────────────────
torchmetrics>=1.3,<2.0

# ── Utilities ───────────────────────────────────────────────────────────────
tqdm>=4.66,<5.0
PyYAML>=6.0,<7.0
python-dotenv>=1.0,<2.0
requests>=2.31,<3.0
rich>=13.0,<14.0

# ── Jupyter ─────────────────────────────────────────────────────────────────
jupyterlab>=4.0,<5.0
ipywidgets>=8.0,<9.0

# ── Dev / linting ────────────────────────────────────────────────────────────
black>=24.0,<25.0
ruff>=0.2,<1.0
isort>=5.13,<6.0
pre-commit>=3.6,<4.0

# ── Testing ──────────────────────────────────────────────────────────────────
pytest>=8.0,<9.0
pytest-cov>=4.0,<5.0
