# =============================================================================
# requirements.txt — non-torch dependencies. Install with:
#   pip install -r requirements.txt
#
# PyTorch is intentionally NOT pinned here.
#
#   RunPod / RTX 5090 (Blackwell, sm_120): use a base image that already ships
#   PyTorch >= 2.7 with CUDA 12.8 (cu128). Do NOT pip-install torch — the image
#   provides it. torch 2.1 + cu118 will fail immediately on Blackwell.
#
#   Local CPU/dev (optional): pip install torch torchvision
#       --index-url https://download.pytorch.org/whl/cpu
#
# Verified against installed environment (2026-05-19):
#   torch==2.11.0  torchvision==0.26.0  numpy==2.1.3  pandas==2.2.3
#   transformers==5.7.0  huggingface-hub==1.12.2  h3==4.4.2  Pillow==11.1.0
#   geopandas==1.1.3  wandb==0.26.1  isort==6.0.1
# =============================================================================

# ── Core scientific stack ───────────────────────────────────────────────────
numpy>=2.0,<3.0          # updated: 1.x upper bound was too low, 2.x is installed
pandas>=2.0,<3.0
scipy>=1.11,<2.0
scikit-learn>=1.3,<2.0
matplotlib>=3.7,<4.0
seaborn>=0.12,<1.0

# ── HuggingFace ecosystem ───────────────────────────────────────────────────
transformers>=5.0,<6.0   # updated: 4.x upper bound exceeded, 5.7.0 installed
datasets>=2.17,<3.0
tokenizers>=0.15,<1.0
huggingface-hub>=1.0,<2.0  # updated: 0.x upper bound exceeded, 1.12.2 installed

# ── Geospatial ──────────────────────────────────────────────────────────────
geopandas>=1.0,<2.0      # updated: 0.x upper bound exceeded, 1.1.3 installed
shapely>=2.0,<3.0
pyproj>=3.6,<4.0
geopy>=2.4,<3.0
folium>=0.15,<1.0
h3>=4.0,<5.0             # updated: 4.x API (latlng_to_cell) installed; code handles both
branca>=0.7,<1.0         # folium dependency

# ── Image processing ────────────────────────────────────────────────────────
Pillow>=10.0,<12.0       # updated: <11.0 was too low, 11.1.0 installed
opencv-python>=4.8,<6.0

# ── Experiment tracking ─────────────────────────────────────────────────────
wandb>=0.16,<2.0         # updated: <1.0 too conservative (1.x may release)
mlflow>=2.10,<4.0

# ── Model utilities ─────────────────────────────────────────────────────────
torchmetrics>=1.3,<2.0
packaging>=23.0           # used in visualize.py for version comparisons

# ── Explainability & visualisation ──────────────────────────────────────────
grad-cam>=1.5,<2.0        # pip name for pytorch_grad_cam; used in visualize.py
plotly>=5.18,<6.0

# ── Utilities ───────────────────────────────────────────────────────────────
tqdm>=4.66,<5.0
PyYAML>=6.0,<7.0
python-dotenv>=1.0,<2.0
requests>=2.31,<3.0
rich>=13.0,<15.0

# ── Dataset download (optional) ─────────────────────────────────────────────
# Used in download_data.py for Mapillary image retrieval.
# Install separately: pip install zensvi
# zensvi>=0.1

# ── Jupyter ─────────────────────────────────────────────────────────────────
jupyterlab>=4.0,<5.0
ipywidgets>=8.0,<9.0

# ── Dev / linting ────────────────────────────────────────────────────────────
black>=24.0,<25.0
ruff>=0.2,<1.0
isort>=5.13,<7.0          # updated: 6.0.1 installed, was <6.0
pre-commit>=3.6,<4.0

# ── Testing ──────────────────────────────────────────────────────────────────
pytest>=8.0,<9.0
pytest-cov>=4.0,<5.0

# =============================================================================
# REMOVED from previous requirements (not used in codebase):
#   openai-clip       — not imported; CLIP loaded via HuggingFace transformers
#   open-clip-torch   — not imported; not needed
#   timm              — not imported; EfficientNet-B2 loaded via torchvision.models
#   accelerate        — not imported directly; optional transformers extra
#   einops            — not imported in any code file
#   optuna            — not imported in any code file
#   kaleido           — not imported in any code file
#   albumentations    — not used; augmentations.py uses torchvision.transforms
# =============================================================================
